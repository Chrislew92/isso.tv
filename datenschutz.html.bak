<!DOCTYPE html>
<html lang="de">
<head>
  <meta charset="utf-8">
  <title>Datenschutz � ISSO.TV</title>
  <style>
    :root { --tinte: #0d0d0d; --papier: #fcfcfc; --out: 4px solid var(--tinte); --kreide: #f5f5f5; --mono: 'Courier New', monospace; --fett: 'Arial Black', Arial, sans-serif; }
    body { background: var(--tinte); color: var(--papier); font-family: var(--mono); margin: 0; padding: 40px; display: flex; justify-content: center; }
    .kasten { background: var(--papier); color: var(--tinte); border: var(--out); max-width: 600px; width: 100%; box-shadow: 8px 8px 0 var(--tinte); padding: 30px; }
    h1 { font-family: var(--fett); text-transform: uppercase; margin-top: 0; border-bottom: var(--out); padding-bottom: 10px; }
    h2 { font-family: var(--fett); font-size: 16px; margin-top: 30px; }
    p { line-height: 1.5; }
    a { color: var(--tinte); font-weight: bold; }
    .back { display: inline-block; margin-top: 40px; padding: 10px 15px; border: var(--out); background: var(--tinte); color: var(--papier); text-decoration: none; font-family: var(--fett); box-shadow: 4px 4px 0 var(--tinte); text-transform: uppercase; }
    .back:hover { filter: brightness(1.2); }
    .back:active { transform: translate(4px,4px); box-shadow: 0 0 0 var(--tinte); }
    .platzhalter { background: #ffeaa7; padding: 2px 5px; color: #d63031; font-weight: bold; }
  </style>
</head>
<body>
  <div class="kasten">
    <h1>Datenschutzerkl�rung</h1>
    
    <h2>1. Wir sammeln nichts.</h2>
    <p>Dieses Spiel (ISSO.TV) l�uft vollst�ndig in deinem eigenen Browser (Client-Side). Es gibt keinen Server unsererseits, an den Spielst�nde, Statistiken oder pers�nliche Daten gesendet werden.</p>

    <h2>2. Speicherung auf deinem Ger�t</h2>
    <p>Das Spiel speichert deinen Fortschritt (Geld, Beruf, Tag) ausschlie�lich im <b>Local Storage</b> deines Browsers ab. Diese Daten verlassen dein Ger�t niemals.</p>
    
    <h2>3. Server-Log-Dateien (Hosting)</h2>
    <p>Die Website wird bei einem externen Provider (STRATO) gehostet. Der Provider erhebt und speichert automatisch Informationen in Server-Log-Dateien (z.B. Browsertyp, Betriebssystem, IP-Adresse, Uhrzeit), die dein Browser automatisch �bermittelt. Diese Daten sind f�r uns nicht bestimmten Personen zuordenbar. Eine Zusammenf�hrung mit anderen Datenquellen wird nicht vorgenommen.</p>

    <h2>4. Verantwortlicher</h2>
    <p>
      Christoph Lewandowski<br>
      E-Mail: kontakt@isso.tv
    </p>

    <a href="index.html" class="back">Zur�ck zum Spiel</a>
  </div>
</body>
</html>
