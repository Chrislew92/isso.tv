# Claude Code — ISSO.TV V3

Vor jeder Arbeit `AGENTS.md` vollständig lesen und befolgen. Danach in dieser Reihenfolge lesen: `docs/PROJECT_STATE.md`, `ROADMAP.md`, `STORY_BIBLE.md`, `docs/ARCHITECTURE.md`.

V3 ist die einzige aktive Basis. V2 nur über den archivierten Tag als geprüften Feature-Spender behandeln. Niemals deployen, pushen oder `isso.tv`/Cloudflare verändern, solange der Nutzer dies nicht in der aktuellen Aufgabe ausdrücklich freigibt.

Diese Datei ergänzt `AGENTS.md` nicht um eigene Produktregeln und darf ihm nicht widersprechen.
